# Copyright (c) 2026 Clark Rawlins
# SPDX-License-Identifier: Apache-2.0

# Build directories
build/
build-*/
cmake-build-*/

# IDE files
.cache/
.vscode/
.idea/
*.swp
*.swo
*~

# Compiled files
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# CMake files
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake
!CMakeLists.txt
!vcpkg-overlays/**/*.cmake
!cmake/Kconfig.cmake
vcpkg_installed
# A second, feature-specific dependency tree (e.g. `--x-feature=gcp` into
# `--x-install-root=vcpkg_installed_gcp`). Kept out of the shared tree on
# purpose: `vcpkg_installed` here is a SYMLINK to another worktree's copy,
# and an in-place install prunes whatever the requested feature set does not
# need — which would silently strip that worktree too.
vcpkg_installed_*

# Rust build output (vcpkg overlay ports built via cargo)
vcpkg-overlays/**/target/

# Test outputs
Testing/
CTestTestfile.cmake
test_results/

# Documentation build
doc/html/
doc/latex/
doxygen-output/

# Host-built binaries staged for docker/*/Dockerfile
# (.kiro/specs/chaos-node-host-build/, .kiro/specs/discovery-nodes-host-build/)
# — not source, not committed.
docker/chaos_node/dist/
docker/poco_discovery_node/dist/
docker/dns_discovery_node/dist/
docker/dns_sd_discovery_node/dist/

# Session handoff scratch notes — working-copy only, never committed.
# RESUME.md is a per-machine set of notes-to-self about in-flight work; it
# goes stale the moment anything merges, and it is explicitly not a source
# of truth (its own header says to verify every claim against the tree).
# Tracking it produced merge conflicts on a file nobody reads from git.
RESUME.md

# Claude Code local tooling — working-copy only, never committed.
# `.claude/agents/kfc/` and `.claude/system-prompts/` are vendored
# third-party content (kiro-for-claude-code), so they are exempt from this
# project's copyright header and are not ours to commit; `settings.local.json`
# holds per-machine permission grants. The install is also configured for
# `.claude/specs/`, while every spec in this tree lives in `.kiro/specs/`.
# Ignoring the directory keeps `git add -A` from sweeping it into a commit.
.claude/

# OS files
.DS_Store
Thumbs.db
*.gcov
*.gcov.json.gz
*.gcda
*.gcno

# Python bytecode from the object-store probe scripts
__pycache__/
*.pyc
